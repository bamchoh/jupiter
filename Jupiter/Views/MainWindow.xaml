<Window x:Class="Jupiter.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"        
        xmlns:local="clr-namespace:Jupiter.Views"
        xmlns:Prism="http://prismlibrary.com/"
        Prism:ViewModelLocator.AutoWireViewModel="True"        
        mc:Ignorable="d"
        Title="Jupiter" Height="660" Width="800">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closing">
            <i:InvokeCommandAction Command="{Binding Path=ClosingCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <DockPanel>
        <local:Connection DockPanel.Dock="Top" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="200" />
            </Grid.ColumnDefinitions>
            <local:NodeTree Grid.Column="0"
                                 MouseDoubleClickedCommand="{Binding MouseDoubleClickedCommand}"
                                 AddToReadWriteCommand="{Binding AddToReadWriteCommand}"
                                 ReloadCommand="{Binding ReloadCommand}"
                                 NodeSelectedCommand="{Binding NodeSelectedCommand}"
                                 />
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" />
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <local:SubscriptionView Grid.Row="0" DeleteMonitoredItemsCommand="{Binding Path=DeleteMonitoredItemsCommand}"/>
                <GridSplitter Grid.Row="1" Height="5" VerticalAlignment="Stretch" Width="Auto" HorizontalAlignment="Stretch" />
                <local:OneTimeAccessView Grid.Row="2"
            ReadCommand="{Binding Path=ReadCommand}"
            WriteCommand="{Binding Path=WriteCommand}"
            DeleteCommand="{Binding Path=DeleteOneTimeAccessItemsCommand}" />

            </Grid>
            <GridSplitter Grid.Column="3" Width="5" HorizontalAlignment="Center" />
            <local:NodeInfoDataGrid Grid.Column="4" />
        </Grid>
    </DockPanel>
</Window>
