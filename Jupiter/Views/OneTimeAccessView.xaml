<UserControl x:Class="Jupiter.Views.OneTimeAccessView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:uc="clr-namespace:Jupiter.UserControls"
             xmlns:Prism="http://prismlibrary.com/"
             Prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="One Time Access Items" />
        <Button DockPanel.Dock="Top" Content="Read" Click="Button_Click" />
        <Button DockPanel.Dock="Top" Content="Write" Click="Button_Click_2" />
        <Button DockPanel.Dock="Top" Content="Delete" Click="Button_Click_1" />
        <uc:VariableInfoDataGrid ItemsSource="{Binding OneTimeAccessItems, Mode=OneWay}" SelectedItems="{Binding Path=OneTimeAccessSelectedItems, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
    </DockPanel>
</UserControl>
